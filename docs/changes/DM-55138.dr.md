Add `raErr`, `decErr`, and `ra_dec_Cov` columns to the DRP `DiaObject` table (`drp_base.yaml`, `lsstcam.yaml`), mirroring the APDB `DiaObject` schema and propagating the per-object position uncertainties produced by the `ap_meanPosition` plugin.
