Added an initial base "column dictionary" schema for SSO data
  This was based on the current apdb.yaml, which makes it consistent with
  the schema used for alert issuance.  On this ticket the new base file
  (sso_base.yaml) is not yet used as input to any concrete schema.
